# 将讲稿草案实现为独立 Beamer sections

依据 `draft/draft.md`，按顺序实现 `sections/item-predicate.tex`、`sections/review-wo-predicates.tex` 和 `sections/review-predicates.tex`。每个文件只包含与其主题相关的 Beamer frame；采用现有 SimplePlus 主题、`newcommands.tex` 中的记号及 `figs/` 中已经准备的文献图。每页保持投影友好：以图、短语、关键词、定理级结论为主，避免长段落和技术推导。每完成一个 section，编译主报告生成 PDF，并以单独提交推送。保留主文档已有的 `\\input` 骨架及其余用户文件。
