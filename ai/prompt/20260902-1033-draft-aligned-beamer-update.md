# Draft-aligned Beamer update

Update `sccp2026-predicates.tex` and its included section files from the current
`draft/draft.md`.  Preserve the existing Beamer template, citation mechanism,
notation macros, and supplied figure assets.  Use each draft heading verbatim as
the title of its corresponding frame.  Present the review as a compact,
visual-first narrative: item-only foundations, their downstream uses, the
predicate-aware literature, and the resulting problem.  Keep LaTeX source
readable through conventional indentation and line breaking; compile the root
document and check the rendered slides for fit, citations, and visual defects
before committing the source and PDF.
