% pred-review.tex

\documentclass[tikz, border=2pt]{standalone}

\usepackage[dvipsnames]{xcolor}
\usetikzlibrary{arrows.meta, positioning, shadows, shapes.geometric}

\begin{document}
  \begin{tikzpicture}[
      >=Latex,
      font = \large,
      box/.style={
        draw = MidnightBlue,
        rounded corners = 4pt,
        fill = MidnightBlue!5,
        align = center,
        inner sep = 2mm,
      },
      sticky note/.style={
        draw = Goldenrod!80!black,
        fill = Goldenrod!18,
        align = center,
        font = \small\itshape,
        inner xsep = 2mm,
        inner ysep = 1.5mm,
        drop shadow={shadow xshift=1pt, shadow yshift=-1pt, opacity=.35},
      },
    ]
    \node[ellipse, draw = BrickRed,
      fill = BrickRed!5, minimum height = 1.2cm] (c)
      {Characterizations};
    \node[box, below left = 1.5cm and -0.5cm of c] (s) {Specs};
    \node[box, below right = 1.5cm and -0.5cm of c] (g) {Depgraphs};
    \node[box, right = 1.5cm of c] (a) {Downstream\\Analysis};
    \node[sticky note, rotate = 2, anchor = south] (c-note)
      at ([yshift = -0.5mm]c.north)
      {does not officially exist};
    \node[sticky note, rotate = -2, anchor = north] (s-note)
      at ([yshift = 0.5mm]s.south)
      {unclear relation to depgraph};
    \node[sticky note, rotate = 2, anchor = north] (g-note)
      at ([yshift = 0.5mm]g.south)
      {no uniform defs};
    \draw[-Latex, very thick, MidnightBlue] (c) -- (s);
    \draw[-Latex, very thick, MidnightBlue] (c) -- (g);
    \draw[-Latex, very thick, MidnightBlue] (c) -- (a);
    \draw[-Latex, <->, very thick, MidnightBlue, dashed] (s) -- (g);
  \end{tikzpicture}
\end{document}
