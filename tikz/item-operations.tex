% item-operations.tex

\documentclass[tikz, border=2pt]{standalone}

\usepackage[dvipsnames]{xcolor}
\usetikzlibrary{positioning}

\begin{document}
  \begin{tikzpicture}[
      font = \large,
      op/.style={
        draw = MidnightBlue,
        rounded corners = 5pt,
        fill = MidnightBlue!5,
        text width = 31mm,
        minimum height = 12mm,
        align = center,
      },
    ]
    \node[op] (r) {\textbf{Read}$(x,v)$};
    \node[op, below = 1cm of r] (w) {\textbf{Write}$(x,v)$};
  \end{tikzpicture}
\end{document}